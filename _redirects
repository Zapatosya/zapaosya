# Cloudflare Pages — SPA fallback para productos
#
# Solo una regla: cualquier URL que NO sea un archivo real → index.html
# Esto permite que /diesel-azul, /kley-cafe, etc. carguen index.html
# y el JS abra el producto correcto.
#
# Las páginas legales (sobre-nosotros.html, devoluciones.html, terminos.html,
# privacidad.html) son archivos físicos en el repo y se sirven directamente
# por Cloudflare antes de aplicar esta regla, así que NO entran en el catch-all.

/*  /index.html  200
